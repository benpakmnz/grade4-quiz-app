# אסטרטגיית המטמון לשאלות AI

## מטרה
למנוע מצב של fallback ולהבטיח שתמיד יהיו מספיק שאלות AI במטמון.

## פרמטרים
- **CACHE_MIN_SIZE**: 10 שאלות - הסף שמתחתיו מתחילה טעינה חדשה
- **CACHE_BATCH_SIZE**: 10 שאלות - כמות השאלות שנטענות בכל פעם
- **INITIAL_CACHE_SIZE**: 10 שאלות - טעינה ראשונית כשהאפליקציה עולה

## תהליך הטעינה

### 1. טעינה ראשונית (App Start)
כשהאפליקציה עולה, מתחילה טעינה אוטומטית של 10 שאלות לכל יחידת לימוד:
- **בעיות מילוליות**: אחרי 1 שניה
- **חקר נתונים**: אחרי 5 שניות
- **צורות**: אחרי 9 שניות

הזמנים מפוזרים כדי למנוע עומס על ה-API.

### 2. טעינה אוטומטית (Auto-Refill)
כשמשתמש מושך שאלה מהמטמון:
1. השאלה מוסרת מהמטמון (`shift()`)
2. בודקים את גודל המטמון
3. **אם המטמון <= 10 שאלות**: מתחילה טעינה של 10 שאלות נוספות ברקע
4. המשתמש ממשיך לענות על שאלות מהמטמון הקיים

### 3. מצב חירום (Empty Cache)
אם המטמון ריק לחלוטין (לא אמור לקרות):
1. מנסים לטעון 10 שאלות באופן מיידי (await)
2. אם הטעינה הצליחה - מחזירים שאלה מהמטמון החדש
3. אם הטעינה נכשלה - משתמשים ב-fallback כמוצא אחרון

## דוגמה לתהליך

```
התחלה: Cache = 0
↓
טעינה ראשונית: Cache = 10
↓
משתמש עונה על 1 שאלה: Cache = 9 (עדיין מעל הסף)
משתמש עונה על 2 שאלות: Cache = 7 (עדיין מעל הסף)
↓
משתמש עונה על שאלה נוספת: Cache = 6 (עדיין מעל הסף)
↓
משתמש עונה על שאלה נוספת: Cache = 5 (עדיין מעל הסף)
↓
משתמש עונה על שאלה נוספת: Cache = 4 (מתחת לסף!)
🚀 מתחילה טעינה של 10 שאלות ברקע
↓
משתמש ממשיך לענות: Cache = 3, 2, 1...
↓
טעינה מסתיימת: Cache = 1 + 10 = 11
↓
חוזר למצב יציב
```

## יתרונות
✅ תמיד יש מלאי של שאלות AI
✅ אין המתנה למשתמש
✅ fallback משמש רק במקרי חירום
✅ טעינה מפוזרת מונעת עומס על ה-API
✅ שאלות לא חוזרות על עצמן

## לוגים
- ✅ `Using cached question` - שימוש בשאלה מהמטמון
- ⚠️ `Cache at X, triggering refill` - מתחיל טעינה חדשה
- 🔄 `Starting to preload` - טעינה מתחילה
- ❌ `No cached question` - מטמון ריק (לא אמור לקרות)
- 🚀 `Starting immediate preload` - טעינה מיידית במצב חירום
